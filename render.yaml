services:
  - type: web
    name: contrat-coaching
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: COACH_PASSWORD
        value: MNS-12345-MNS
      - key: PYTHON_VERSION
        value: 3.11.6
